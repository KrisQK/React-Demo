const Button = () => {
    const styles = {
        backgroundColor: "aqua",
        color: "white",
        padding: "10px 20px",
        border: "none",
        borderRadius: "5px",
        fontSize: "16px",
        cursor: "pointer",
    };
    return <button style={styles}>Button</button>;
};

export default Button;
